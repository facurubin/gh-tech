export interface IUsers {
  id: string;
  username: string;
  type: string;
  avatarUrl: string;
}

export interface IProfile {
  name: string;
  blog: string;
  bio: string;
  location: string;
  avatar_url: string;
}
